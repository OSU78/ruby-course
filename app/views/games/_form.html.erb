<!-- app/views/games/_form.html.erb -->

<%= raise 'Variable game est null!' if game.nil? %>
<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :'Nom du jeu' %>
    <%= form.text_field :name, id: :game_name %>
  </div>

  <div class="field">
      <%= form.label :username, "Ton prénom", class: "graycolor" %>
      <%= form.text_field :username, id: :game_username, placeholder: "Permet à tes amis de te retrouvé.." %>
  </div>
    


  <div class="field">
    <%= form.label :"L'url du poster du jeu" %>
    <%= form.text_field :banner, id: :game_banner %>
  </div>

  <div class="field">
  <%= form.label :"Prix du jeux" %>
  <%= form.number_field :price, id: :game_price %>
  
    </div>

    <div class="flex row center">

     <div class="actions flex row center gap10">
    <%= form.submit class: 'button submit-button' %>
   <%# <%= link_to 'Supprimer', game, method: :delete, data: { confirm: 'Êtes-vous sûr ?' }, class: 'button delete-button' %> %>

  </div>
  </div>

    
  <% end %>
